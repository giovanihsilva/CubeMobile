using UnityEngine;

public class AutoDestroy : MonoBehaviour
{

    [Header("Auto Destroy Settins")]
    [Range(0f, 10f)]
    [SerializeField] private float destroyTime = 5f;













    private void Start()
    {
        Destroy(gameObject, destroyTime);
    }













































































































































}
